<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="danielfranke.com.cst2335_androidlabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".StartActivity"
            android:label="@string/activityNameStart">

            <!--activity to start first-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Login"
            android:label="@string/activityNameLogin">
        </activity>

        <activity
            android:name=".ListItems"
            android:label="@string/activityNameListItems">
        </activity>

        <activity
            android:name=".ChatWindow"
            android:label="@string/activityNameChatWindow">
        </activity>

        <activity
            android:name=".WeatherForecast"
            android:label="@string/activityNameWeatherForecast">
        </activity>

    </application>

</manifest>